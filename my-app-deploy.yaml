kind: Service
apiVersion: v1
metadata:
  name: my-app-Service
spec:
  type: NodePort
  selector:
    app: app
  ports:
  - protocol: TCP
    port: 8080
    NodePort: 30950
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  labels:
    app: app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      containers:
      - name: app
        image: $DOCKER_IMAGE_NAME:$BUILD_NUMBER
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 10
          timeoutSeconds: 1
          periodSeconds: 10
        resources:
          requests:
            cpu: 300m

            


